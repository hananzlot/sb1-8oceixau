import React from 'react';
import { View } from 'react-native';

export const MapView = props => <View {...props} />;
export const Marker = props => <View {...props} />;
export const Circle = props => <View {...props} />;

export default { MapView, Marker, Circle };